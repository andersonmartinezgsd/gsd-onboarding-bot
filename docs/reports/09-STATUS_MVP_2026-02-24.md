# Reporte de Status MVP - 2026-02-24

## Resumen ejecutivo

Estado general del MVP: **funcional para salida controlada** en `DEV`, con módulos operativos de agentes, entrenamiento, chat de orientación, chatbot, tareas, workflows, APIs y configuración.

## Snapshot técnico (fuente: `data/DAMROSAI.db`)

- Agentes totales: `87`
- Agentes activos: `13`
- Perfiles de entrenamiento (`app_agent_profiles`): `87`
- Tareas registradas: `8`
- Blueprints de workflow (`app_workflow_blueprints`): `2`
- Integraciones (`data_sources`): `9`
- Config runtime API (`app_api_runtime_config`): `1`
- Favoritos de agentes (`app_agent_favorites`): `0`
- Snapshots IQ (`agent_iq_snapshots`): `87`
- Eventos IQ (`agent_iq_events`): `88`

Cobertura de perfil por agente (`app_agent_profiles`):

- Con documentos: `87/87`
- Con skills: `87/87`
- Con especialidades: `87/87`
- Con funciones: `87/87`

Promedio por agente:

- Documentos: `2.34`
- Skills: `4.0`
- Especialidades: `4.0`
- Funciones: `3.0`

IQ snapshot actual:

- IQ promedio: `114.86`
- IQ máximo: `127.08`
- IQ mínimo: `104.35`

## Estado por módulo MVP

- Dashboard modular (header, estadísticas, paneles): **OK**
- Lista de agentes + modal Ver + chat orientación: **OK**
- Chatbot con estrategia DB -> LLM (Ollama) -> Web fallback: **OK**
- Tasks (seguimiento, bloqueos, escalar): **OK**
- Workflows (local blueprint + publicación a n8n): **OK**
- APIs (integraciones externas, test de conexión): **OK**
- API Config (lectura, edición y test): **OK**
- Reporte Training / IQ Realisa: **OK**
- Endpoint demo versions / launch readiness: **OK**

## Riesgos / gaps para lanzamiento

1. Protección técnica de ramas en GitHub:
   - Intento de activar branch protection vía API devolvió `403`.
   - Causa: repositorio privado sin feature habilitada en plan actual.
2. Convivencia de archivos DB canónica y legacy:
   - Existen `AMROSAI.db` y `amrosai.db` en repo.
   - Riesgo de confusión en entornos case-insensitive.
3. Hardening operativo:
   - Completar observabilidad de latencia y errores de integraciones externas bajo carga.

## Recomendaciones inmediatas (pre-lanzamiento)

1. Mantener flujo obligatorio: `feature/* -> dev -> staging -> main`.
2. Aplicar regla DB por rama en todos los scripts/operaciones:
   - `MAIN -> AMROSAI.db`
   - `STAGING -> SAMROSAI.db`
   - `DEV -> DAMROSAI.db`
3. Ejecutar smoke test por ambiente tras cada merge:
   - `/health`
   - `/app/data/dashboard`
   - `/app/data/tasks`
   - `/app/data/workflows`
   - `/app/data/apis`
   - `/app/data/api-config`
4. Completar validación de n8n y Ollama en staging antes de promover a main.

## Conclusión

El MVP está en estado **release candidate** para lanzamiento controlado, con funcionalidad core estable en entorno `DEV` y listo para promoción ordenada a `STAGING` y `MAIN`.
